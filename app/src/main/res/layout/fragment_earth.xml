<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/coordinator_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

<include layout="@layout/include_earth"
    android:layout_gravity="center"
    android:id="@+id/include_earth"
    android:layout_height="match_parent"
    android:layout_width="match_parent" />

    <FrameLayout
        android:layout_gravity="bottom"
        android:id="@+id/frame_text_discription"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="?attr/actionBarSize"
        android:alpha="1.0"
        app:layout_behavior=".utils.CustomBehavior"
        android:background="@color/grey"
        app:behavior_peekHeight="@dimen/behavior_peekHeight">

        <TextView
            android:id="@+id/text_discription_POD"
            style="@style/my_text_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:textColor="@color/white" />
    </FrameLayout>
</androidx.coordinatorlayout.widget.CoordinatorLayout>